# Telegram Admin Bot - Инструкция по установке

## 🚀 Быстрый старт

### 1. Получение токена бота
1. Напишите в Telegram: [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Назовите бота и введите username
4. Скопируйте выданный токен (выглядит как: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2. Узнание Telegram ID администраторов
1. Напишите боту `/start`
2. Откройте логи бота (смотрите консоль)
3. Найдите строку `Unauthorized access attempt from user 123456789`
4. Число после `user` - это Telegram ID
5. Повторите для каждого администратора (если их несколько)

### 3. Конфигурация

```bash
# Скопируйте файл примера
cp .env.example .env

# Отредактируйте .env с вашими значениями
nano .env
```

Заполните:
- `BOT_TOKES` - Telegram ID(s) администраторов, разделены запятой (например: `123456789,987654321`)ther
- `ADMIN_ID` - ваш Telegram ID
- `DOCKER_COMPOSE_PATH` - путь к docker-compose.yml (по умолчанию работает)
- `FAIL2BAN_LOG_PATH` - путь к логам fail2ban

### 4. Установка и запуск

```bash
# Способ 1: Быстрый запуск
bash run.sh

# Способ 2: Ручной запуск
source bin/activate
pip install -r requirements.txt
python "src code/main.py"
```

## 🎯 Функции бота

### 🔄 Статус сервера
- Показывает количество запущенных контейнеров
- Отображает нагрузку на систему (load average)
- Обновляется в реальном времени

### 🚀 Перезагрузка Docker
- Выполняет `docker-compose down`
- Ожидает 5 секунд
- Выполняет `docker-compose up -d`
- Требует подтверждение

### 🚨 Мониторинг Fail2Ban
- Автоматическая проверка каждую минуту
- Уведомление при превышении порога срабатываний
- Показывает последние события

## 🔒 Безопасность

- ✅ Доступ только для авторизованного пользователя (по ADMIN_ID)
- ✅ Все команды логируются
- ✅ Критические операции требуют подтверждения
- ✅ Проверка прав доступа на каждое действие

## 📋 Требования

- Python 3.7+
- Docker & Docker-Compose (установлены на сервере)
- Доступ к логам fail2ban
- Интернет для подключения к Telegram

## ⚙️ Переменные окружения

| Переменная | Описание | Пример |
|-----------|---------|--------|
| `BOT_TOKEN` | Токен бота от BotFather | `123456:ABC-DEF...` |
| `ADMIN_IDS` | Telegram ID(s) администраторов (через запятую) | `987654321,123456789` |
| `DOCKER_COMPOSE_PATH` | Полный путь к docker-compose.yml | `/home/user/docker-compose.yml` |
| `FAIL2BAN_LOG_PATH` | Путь к логам fail2ban | `/var/log/fail2ban.log` |
| `FAIL2BAN_THRESHOLD` | Порог срабатываний (в минуту) | `5` |

## 🛠️ Решение проблем

### Бот не подключается
```bash
# Проверьте токен
curl https://api.telegram.org/bot<ВАШ_ТОКЕН>/getMe
```

### Ошибка доступа к docker-compose
```bash
# Бот должен запускаться от пользователя, входящего в группу docker
sudo usermod -aG docker $USER
newgrp docker
```

### Не видны логи fail2ban
```bash
# Проверьте путь и права доступа
ls -la /var/log/fail2ban.log
# Может потребоваться запустить с sudo или добавить пользователя в группу
```

## 📝 Примеры команд

```
/start          - Показать главное меню
/help           - Справка
```

## 🔗 Полезные ссылки

- [python-telegram-bot Documentation](https://python-telegram-bot.readthedocs.io/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Docker-Compose Docs](https://docs.docker.com/compose/)
- [Fail2Ban Docs](https://www.fail2ban.org/)

## 📄 Лицензия

MIT
